# PhantmOS v3.0 — Python Dependencies
# ─────────────────────────────────────────────────────────

# Core framework
fastapi
uvicorn[standard]
python-multipart
python-dotenv
pydantic
cryptography

# Database
supabase

# HTTP client (used by all API adapters)
httpx

# Scheduling
apscheduler

# Telegram bot
python-telegram-bot

# ── Stage 1: Harvesting ───────────────────────────────────
# (All free APIs — no SerpApi needed)
duckduckgo-search      # context researcher + genuity checker
beautifulsoup4         # html parsing for web scraping

# ── Stage 2: Embeddings ───────────────────────────────────
numpy
sentence-transformers  # local fallback model (paraphrase-MiniLM-L3-v2, ~50MB)
# Jina AI: called via httpx — no package needed
# NOTE: sentence-transformers pulls torch; if RAM is tight on HF Spaces,
#       set JINA_API_KEY and the local model is never loaded.

# ── Stage 3: LLM providers ────────────────────────────────
# Groq: called via httpx — no package needed
# Gemini: called via httpx REST API — no SDK needed
# HuggingFace: called via httpx — no package needed

# ── Stage 4: PDF generation & parsing ──────────────────────
rendercv>=0.3.0        # core PDF generation engine
typst                  # pdf compiler required by rendercv
pypdf                  # text extraction from uploaded master resumes
